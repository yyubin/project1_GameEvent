<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smhrd.js">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JS"
        android:usesCleartextTraffic="true">

        <activity android:name=".ranking"></activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".join" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".MainPage" />
        <activity android:name=".free_board" />
        <activity android:name=".id_find" />
        <activity android:name=".pw_find" />
        <activity android:name=".CreateTeam" />
        <activity android:name=".battle_attend" />
        <activity android:name=".chatting" />
        <activity android:name=".Survival" />
        <activity android:name=".Table" />
    </application>

</manifest>